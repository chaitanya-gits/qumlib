[project]
name = "quantumlib"
version = "0.1.0"
description = "A minimal quantum computing simulation library"
authors = [{ name="Your Name" }]
dependencies = ["numpy"]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
